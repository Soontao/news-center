{
  "name": "news-center",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "test": "node --test ./test/**",
    "lint": "eslint src/** --fix",
    "release": "npx standard-version --no-verify && git push --follow-tags origin main",
    "format": "npx prettier -w src"
  },
  "engines": {
    "node": ">=20",
    "npm": ">=10"
  },
  "devDependencies": {
    "@sap/cds-dk": "^7.7.2",
    "eslint": "^8.57.0",
    "prettier": "^3.2.5"
  },
  "dependencies": {
    "@sap/cds": "^7.7.3",
    "cds-mysql": "^7.8.1"
  },
  "license": "Apache-2.0"
}
